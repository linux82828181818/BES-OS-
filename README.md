# test-mini-os
haha lol ya dolboeb
укр : це міні ос яку я пишу тільки на ассемблері назва BES-OS версія 0.0.8 поки  що вміє завантажуватися і працює тільки з legacy bios драйверів UEFI поки що не має ще поки що ядро може працювати з клавіатурою 0x60 0x80  працює з VGA(0x000B8000) )
русс : это мини ос которую я пишу только на ассемблере название BES-OS версия 0.0.8 пока умеет загружаться и работает только с legacy bios драйверов UEFI пока еще нет пока ядро ​​может работать с клавиатурой 0x60 0x80 работает с VGA(0x000B8000)
En : This is a mini OS that I am writing entirely in assembly Named BES-OS (version 0.0.8) it currently boots up and works only with legacy BIOS drivers UEFI support is not available yet At this stage the kernel can handle the keyboard (ports 0x60, 0x80) and works with VGA (0x000B8000)
